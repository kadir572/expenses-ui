export enum PaymentMethod {
  card = 'card',
  cash = 'cash',
  twint = 'twint',
}

export enum PurchaseLocation {
  store = 'store',
  online = 'online',
  other = 'other',
}

export enum Interval {
  daily = 'daily',
  weekly = 'weekly',
  monthly = 'monthly',
  yearly = 'yearly',
}
